name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

permissions:
  contents: read

jobs:
  build-and-test:
    name: Build and test
    runs-on: ubuntu-latest

    steps:
      - name: Check out repository
        uses: actions/checkout@v7

      - name: Set up .NET
        uses: actions/setup-dotnet@v6
        with:
          dotnet-version: 10.0.x

      - name: Restore dependencies
        run: dotnet restore Tailord.slnx

      - name: Build solution
        run: dotnet build Tailord.slnx --configuration Release --no-restore

      - name: Run tests
        run: dotnet test Tailord.slnx --configuration Release --no-build --no-restore
